import { Body, Screen, Title } from '@classsync/ui-mobile';

export default function ParentProgress() {
  return (
    <Screen>
      <Title>Progress</Title>
      <Body>Placeholder — grades and homework come next.</Body>
    </Screen>
  );
}
